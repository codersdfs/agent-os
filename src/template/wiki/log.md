# Wiki log

Append-only history of what happened and when. Every entry starts with
`## [YYYY-MM-DD] <action> | <title>` so the log stays greppable.

## [{{today}}] init | workspace scaffolded

- Created by create-agent-os: `raw/`, `wiki/`, `skills/`, `AGENTS.md`, index.
- Git initialized with the first commit.
